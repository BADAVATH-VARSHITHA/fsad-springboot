[phases.setup]
nixPkgs = ["jdk17_headless"]

[phases.install]
cmds = []

[phases.build]
cmds = ["echo 'Using pre-built JAR - skipping Maven'"]

[start]
cmd = "java -Xmx512m -jar fsad-springboot-main/springboot/target/backend-0.0.1-SNAPSHOT.jar"
